#!/bin/bash

# Install Ubuntu Restricted Extras
function codecs {
if (whiptail --title "Multimedia Codecs" --yesno "Установить Ubuntu Restricted Extras для воспроизведения мультимедиа?" 10 60) then
    # Install Ubuntu Restricted Extras
    show_info 'Установка...'
    show_warning 'Требуется привилегии суперпользователя'
    sudo apt-get install -y ubuntu-restricted-extras libavcodec-extra
    # Done
    show_success 'Готово.'
    whiptail --title "Завершено" --msgbox "Установка завершена." 8 78
    main
else
    clear && main
fi
}
